def train_model(X_train, y_train, model):\n    '''Trains the provided machine learning model on the training data.'''\n    model.fit(X_train, y_train)\n    return model\n\ndef evaluate_model(model, X_test, y_test):\n    '''Evaluates the model using the provided test data and returns the accuracy score.'''\n    from sklearn.metrics import accuracy_score\n    predictions = model.predict(X_test)\n    return accuracy_score(y_test, predictions)\n\ndef save_model(model, filepath):\n    '''Saves the trained model to the specified filepath.'''\n    import joblib\n    joblib.dump(model, filepath)\n\ndef load_model(filepath):\n    '''Loads a saved model from the specified filepath.'''\n    import joblib\n    return joblib.load(filepath)